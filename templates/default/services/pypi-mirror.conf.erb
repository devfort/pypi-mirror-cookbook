#!upstart
description "Update pypi mirror"

start on started pypi-mirror-shim
stop on stopping pypi-mirror-shim

task
respawn

setuid <%= node.pypi_mirror.user %>

env USER=<%= node.pypi_mirror.user %>
env HOME=/home/<%= node.pypi_mirror.user %>

exec bandersnatch mirror
